import GameState from "../gamestate/GameState";
import Engine from "./Engine";

describe('game' , () => {
  it('keeps track of the current and all previous states', () => {
      // console.log(new Engine().playTillLevel(GameState.initialBoard(), 5));
  });

});